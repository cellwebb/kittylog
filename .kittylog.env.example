# Example configuration for kittylog
#
# CONFIGURATION HIERARCHY (highest precedence first):
# 1. Environment variables (KITTYLOG_MODEL=..., etc.)
# 2. Project .kittylog.env (this file in your project root)
# 3. Project .env file
# 4. Global ~/.kittylog.env (created by `kittylog init`)
#
# SETUP:
# - Run `kittylog init` to create your global config at ~/.kittylog.env
# - Copy this file to .kittylog.env in your project root for project-specific settings
# - Edit the values below and uncomment the lines you want to use

# AI Provider Configuration
# Uncomment and set your preferred model:
# KITTYLOG_MODEL=anthropic:claude-3-5-haiku-latest
# KITTYLOG_MODEL=cerebras:qwen-3-coder-480b
# KITTYLOG_MODEL=groq:openai/gpt-oss-120b
# KITTYLOG_MODEL=ollama:gpt-oss:20b
# KITTYLOG_MODEL=openai:gpt-5-mini

# API Keys (only one needed depending on your chosen model)
# ANTHROPIC_API_KEY=your-anthropic-api-key
# CEREBRAS_API_KEY=your-cerebras-api-key
# GROQ_API_KEY=your-groq-api-key
# OPENAI_API_KEY=your-openai-api-key
# OPENROUTER_API_KEY=your-openrouter-api-key
# ZAI_API_KEY=your-zai-api-key

# OLLAMA_HOST=http://localhost:11434 (or remote server)

# Additional Configuration Options (uncomment to override defaults):
# KITTYLOG_TEMPERATURE=1.0                   # AI response creativity (0.0-2.0)
# KITTYLOG_MAX_OUTPUT_TOKENS=2048            # Maximum tokens in AI response
# KITTYLOG_RETRIES=3                         # Number of API retry attempts
# KITTYLOG_WARNING_LIMIT_TOKENS=32768        # Token count warning threshold
# KITTYLOG_LOG_LEVEL=WARNING                 # Logging level

# Boundary Detection Configuration (NEW FEATURES):
# KITTYLOG_GROUPING_MODE=tags              # How to group commits: tags|dates|gaps
# KITTYLOG_GAP_THRESHOLD_HOURS=4.0         # Time gap threshold for gap-based grouping
# KITTYLOG_DATE_GROUPING=daily             # Date grouping: daily|weekly|monthly

# Examples of boundary detection modes:
# tags mode (default): Use git tags to create changelog sections
# dates mode: Group commits by date (daily/weekly/monthly)
# gaps mode: Detect natural breaks in commit timing (e.g., 4+ hour gaps)
